package jsweet.dom;
/**  <p>The <code>localStorage</code> property allows you to access a local <code>Storage</code> object. <code>localStorage</code> is similar to <code>sessionStorage</code>. The only difference is that, while data stored in <code>localStorage</code> has no expiration time, data stored in <code>sessionStorage</code> gets cleared when the browsing session ends—that is, when the browser is closed.</p>  */
@jsweet.lang.Interface
public abstract class WindowLocalStorage extends jsweet.lang.Object {
    public Storage localStorage;
}

